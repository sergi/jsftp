language: node_js
node_js:
    - "0.10"
    - "0.8"
    - "0.6"

before_script:
    - "npm install --dev"

install:
  - sudo pip install pyftpdlib --use-mirrors
  - sudo pip install PyOpenSSL --use-mirrors
